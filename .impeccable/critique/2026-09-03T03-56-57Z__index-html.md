---
target: index.html
total_score: 38
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 0
timestamp: 2026-09-03T03-56-57Z
slug: index-html
---
⚠️ DEGRADED: single-context (no Agent/subagent spawn tool exists in this orchestrator's toolset). Sequencing invariant preserved manually: Assessment A (design-director review from source + DESIGN.md only) was written in full and sealed to /tmp/impeccable-A-sealed.md BEFORE the detector run or any browser evidence entered context; the two assessments never saw each other's output and were synthesized only after both were complete.

Method: degraded dual-pass (A: design review, source + DESIGN.md, no browser · B: detector + Playwright/Chromium fake-mic evidence at /tmp/vox-verify/ against the live server on :8000 — the honest live route, since macOS TCC silences the real mic in an in-app browser).

# Design Critique — VOXCHAIN / index.html

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of system status | 4 | Prior residual closed and verified live: sentence 13.6px in cold/Live/BYPASSED states, readouts 12px, deck one 64px row |
| 2 | Match system / real world | 4 | "sound" (Simple) vs "preset" (Advanced) dual naming persists — view-scoped, taught, acceptable |
| 3 | User control and freedom | 4 | Visible undo verified live end to end; ONE residual: no redo (pop-only stack) |
| 4 | Consistency and standards | 4 | 5 chips verified live; one search vocabulary (name + description + public tags) across views |
| 5 | Error prevention | 4 | Unchanged excellence: headphone gate, mutation gates, locked limiter, two-step delete, watchdog |
| 6 | Recognition rather than recall | 4 | Alt+Arrow taught on grip focus via the register help line, single-sourced with the aria-label — verified live |
| 7 | Flexibility and efficiency | 3 | The one heuristic no round has visited: no redo, no search-focus key, no batch gestures |
| 8 | Aesthetic and minimalist design | 4 | Refinements landed as subtraction + reuse; zero new chrome, the register discipline intact |
| 9 | Error recognition and recovery | 4 | Unchanged excellence: operator error copy with next action, latched autosave warning, asked-vs-allowed refusals |
| 10 | Help and documentation | 3 | Text manual now honest and lint-pinned — but its only SCREENSHOT still shows the retired cord board beneath corrected alt text; Copy-link undocumented |
| **Total** | | **38/40** | Good — closing |

Theme of the remaining deductions: the prose contract is now enforced (README verbs lints in CI), but the PICTURE contract is not; and efficiency (redo, batch, hotkeys) is the one axis no refinement round has visited. All four queued refinements landed true to their findings — verified in source AND live — and nothing regressed.

## The four refinements — did each land?

1. **README + doc-verb lint (P2 #1): LANDED, one material residue.** The text teaches the shipped surface (grip drag + Alt+Left/Alt+Right, Escape revert, searchable list, the 5 real chips, honest mic-dropdown prose) and tests/test-readme-gestures.js pins retired phrases as exact strings and shipped verbs as regexes. RESIDUE: docs/screenshot.png still shows the RETIRED cord-era UI — verified by inspecting the image: free 2D board, drawn patch cables, MIC IN/OUT jack rings, a PALETTE sidebar, the preset dropdown+Load flow — directly beneath alt text describing the current ordered-row console. Image and caption now contradict in the manual's first screenful.
2. **Human structural-edit toast (P2 #2): LANDED TRUE, verified live.** Palette add → one `.agent-toast` card, `data-human-edit="true"`, label "Add Reverb", Undo key present, region aria-label "Change notifications"; a knob commit raised NO toast (count unchanged); the toast's Undo key restored the chain with NO "Undone" annotation (silent-restore contract held); announcement is derived from the edit's shape (`humanEditAnnounces`) beside the ONE label derivation — no second vocabulary.
3. **Chip prune + search unification (P2 #3): LANDED TRUE, verified live.** The chips row renders exactly All / Warm / Big echo / Funny / Clean & clear (5) — pruned by measured coverage (10/7/5/5 kept, both 4s cut, table recorded in source, guarded by test K4); Advanced's haystack now joins description and strips the technique axis symmetrically with Simple's `publicTags`, keeping its expert fields one-way.
4. **Alt+Arrow hint + deck sentence step (P3 #4/#5): LANDED TRUE, verified live.** `REORDER_KEY_HINT` is one string feeding both the grip's aria-label and the register's help line; focus pushes it, blur pops it (falling back to the card's hover preview), and it re-pushes after each accepted Alt+Arrow render while focus follows the moved card's rebuilt grip. Two live Alt+ArrowRight presses walked [gain, compressor, eq, …] → [compressor, eq, gain, …] with operator-labeled toasts ("Move Compressor to the start", "Move EQ after Compressor"), the one-button undo invariant visible (older toast's key removed as the newer lands), and Cmd+Z restoring one step silently. The sentence computes 13.6px in every engine state with readouts at 12px and the cap at 22.5rem; DESIGN.md's typography record was amended in the same stroke.

## Design Specificity Verdict

LLM assessment: authored for this product, emphatically. The four refinements are each REUSE, not addition — the toast machinery, the register preview stack, the type tokens, the test precedents — so the surface's chrome count did not grow while its legibility did. The announcement decision is derived from the edit's own shape rather than call sites; the chip prune is a recorded measurement with a guarding test; the hint is one string with two mouths; the sentence's step re-ranks an existing token ladder instead of adding a size. The deeper machinery remains the most product-native on the page: the gated print tier calibrated to survive the 0.55 recede, the cold face's persistence-read strip, the honesty gates (lamps feed dark under bypass; the scope flattens alone). Web-app-shaped residues: the Advanced presets searchable list (conventional, subordinate) and the plain safety modal (documented off-chassis exception).

Deterministic scan (4 findings, exit 2 — identical set to the prior run, zero new): ALL false positives against DESIGN.md's documented exceptions — (1) "1px border + 48px shadow" is the enumerated off-chassis modal lift (safety dialog + toasts, Shadow Vocabulary); (2) "roboto" is a fallback inside the committed platform stack (no-webfonts rule); (3) repeating-gradient stripes are the one documented disabled-grammar hatch; (4) rgb(237,232,222) is the legacy `--text-primary` token on the never-rendered sr-only #stop-hint span — false positive for drift, but the legacy pre-redesign token block still coexists with the `--pm-*` register (hygiene, never rendered). Detector limits: all findings line:0 (they live in styles/main.css, not index.html).

Browser evidence (Playwright + Chromium, --use-fake-device-for-media-stream, server on :8000): engine reaches Live through the headphone gate; zero app console errors/pageerrors across cold → live → add → undo → param tweak → grip focus → two Alt+Arrow moves → Cmd+Z → bypass walk → restore; deck sentence reads "Bypassed — effects off" with "Bypass: ON" and stays 13.6px; the deck holds one 64px row at 1600px. HONEST HARNESS CAVEAT (recorded concretely): the first Alt+Arrow attempt FAILED, and the failure was the environment's, not the app's — this machine's in-app overlay script (localhost:8400/live.js, functions focusSteerChat/syncPageChatFocus) steals focus to a chat input ~130ms after any programmatic focus, popping the hint and starving the keydown target (captured via a focusout stack trace). With the overlay network-blocked (context.route abort — the run's single console error is that block itself), the full keyboard walk verifies as described. Any future focus-dependent evidence in this environment must neutralize that overlay or it will falsify results.

## What's Working

1. The visible human undo path is complete and honest: structural edits announce with their own operator label and the shared Undo key, param tweaks and IN/BYP stay silent by a principled rule, restores are silent and guarded, and the keyboard route (Ctrl/Cmd+Z) rides the same stack — verified live at every step.
2. Refinement by subtraction and reuse: the chips row got smaller by measurement, the announcement got visible by reusing the toast, the hint got discoverable by reusing the help line, the sentence got readable by re-ranking tokens. No new vocabulary anywhere; the Pattern Machine is undiluted.
3. The manual became a contract: retired phrases exact-string-banned, shipped verbs regex-required, in CI on the token-contrast precedent. Documentation drift can no longer survive a UI generation silently — the prior critique's own closing question, answered yes.

## Priority Issues

1. [P2] The README's only screenshot still teaches the retired surface — patch-cord board, jack rings, PALETTE sidebar, preset dropdown+Load — beneath alt text that describes the current console. A sighted operator skims the picture, not the prose; the manual's single image is its most-read element and it is one UI generation stale. Fix: regenerate the capture from the live surface (one shot per view), and consider pinning the image's provenance the way the lint pins verbs. ($impeccable document)
2. [P3] No redo. The undo stack is pop-only; Cmd+Shift+Z passes through untouched by design. An over-undone drag or Try must be reconstructed by hand at the worst moment. ($impeccable harden)
3. [P3] The <900px Advanced deck squeezes the status sentence to ~67px so "Bypassed — effects off" ellipsizes (pre-existing, measured identical at 12px and 13.6px by the entry-4 round; Simple reads it in full). The distance-readability gate now holds at every width in the default view but not in Advanced. ($impeccable layout)
4. [P3] Faint-lamp luminance inversion persists (unqueued prior minor): opacity maps 0.45..1, so the first flicker of signal paints a chevron dimmer than the resting dead one — light reads as dim at the bottom ~10 dB. ($impeccable colorize)
5. [P3] The armed/QUEUED state is visual-only (unqueued prior minor): the dashed edge and QUEUED legend never announce, so a screen-reader operator doesn't know arming changed the stage's promise until Start fires. ($impeccable clarify)

## Persona Red Flags

Alex (power user): still no redo, no batch gestures (fold-all, bypass-all), no search-focus hotkey; Advanced's search now matches descriptions like Simple's, and the Alt+Arrow walk is discoverable without a screen reader.

Sam (screen-reader + keyboard): the grip's aria-label and the visible hint are ONE string — parity by construction, verified live; the toast region's "Change notifications" relabel is honest about its two authors; structural edits announce once through the live region. Remaining: armed/QUEUED is silent to AT (P3 #5).

The non-engineer live operator: his own "Add Reverb" card now appears with an Undo key in his vocabulary, the manual teaches "nothing you do by hand is a one-way door", and the 13.6px sentence makes the mid-show word readable at distance. Remaining: the manual's PICTURE still shows him a board that no longer exists (P2 #1); at 390px in Advanced his BYPASSED sentence ellipsizes (P3 #3).

## Minor Observations

- Alt+Arrow move labels name the FIRST DISPLACED module, not the grabbed one (live: pressing Alt+Right on Gain's grip announced "Move Compressor to the start") — an accurate description of the transformation, but a surprising subject for the operator who moved a different card.
- Legacy `--text-primary: #EDE8DE` token block (styles/main.css ~line 41) still coexists with the `--pm-*` register — never rendered, pure hygiene, detector-visible every run.
- Three Start verb grammars persist: "Press Start to hear it" (cold face) vs "try a sound / try <name>" (gate note, armed) vs "power on" (deck hints).
- PRODUCT.md's "Terminology in the current production UI" still lists MIC IN / OUT anchors and "Palette" — retired/renamed; the README lint doesn't read PRODUCT.md, and neither does any test.
- README does not yet document Copy-link preset sharing (a shipped, competition-relevant feature), flagged and deferred in the refinement log.
- The human toast is visually identical to an agent toast; the label grammar (verb-first operator labels vs agent sentence summaries) disambiguates, but only if read.
- In-source "Refinement entry 3" comments date from the 2026-08-28 round and no longer match refinement.md's numbering — harmless comment archaeology.
- The param-tweak undo label uses a typed "→" ("Reverb mix 30 → 45") — fine in an off-chassis toast; keep it out of on-chassis registers under the Drawn-Mark Rule.

## Questions to Consider

1. The toast region now speaks for two authors with no visual author mark — is the label grammar the permanent author signal, or does a human edit deserve one word of provenance ("you added Reverb") once toasts are several deep?
2. The verb lint keeps the manual's WORDS honest — what keeps its PICTURE honest? Should the screenshot become a regenerable build artifact (a capture script with a provenance line) rather than a memory?
3. The Move label names the transformation, not the grabbed card — is the subject of an undo label what the operator DID or what the chain BECAME, and should the two ever differ?
