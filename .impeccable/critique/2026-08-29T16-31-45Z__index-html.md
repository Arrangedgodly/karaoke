---
target: index.html
total_score: 33
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 0
p2_count: 3
timestamp: 2026-08-29T16-31-45Z
slug: index-html
---
⚠️ DEGRADED: single-context (no sub-agent/Task tool exposed in this session). Assessment A (design review) was completed and recorded in full before any detector output entered context; Assessment B (detector + browser evidence) ran second. Closing run of the refinement round; baseline 27/40 (.impeccable/critique/2026-08-29T05-41-33Z__index-html.md).

# Design Critique — index.html (Karaoke Chain Builder) — Closing Run

Mode: Operate. Target: index.html + styles/main.css, src/canvas.js, src/param-controls.js, src/presets-ui.js, src/main.js, src/agent-ui.js, src/meters.js, src/meter-taps.js. Intent sources: PRODUCT.md, DESIGN.md ("The Touring Rack"), .impeccable/surfaces/index-html.md, .impeccable/design.json.

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | Status aria-live, derived from context+track; bypass shows on canvas (dim + BYPASSED line). Residue: OUT meter below the fold in default expanded live state. |
| 2 | Match System / Real World | 3 | Plain-language layer on all 14 params; visible labels remain engineering-first, help hover-only. |
| 3 | User Control and Freedom | 3 | No undo for manual edits; preset delete permanent; Save As… native prompt. |
| 4 | Consistency and Standards | 4 | One vocabulary; bypassed canvas reuses gated dim; native prompt()/confirm() the only off-system moments. |
| 5 | Error Prevention | 3 | Lifecycle handling, serialized switches, watchdog; +24 dB gain and limiter removal mid-show unguarded. |
| 6 | Recognition Rather Than Recall | 3 | Param help one hover away; node addition pointer-only. |
| 7 | Flexibility and Efficiency | 3 | No keyboard add/reorder, no numeric fader entry. |
| 8 | Aesthetic and Minimalist Design | 3 | Occlusion defect gone, 11px floor holds; OUT still ends a long scroll. |
| 9 | Error Recovery | 4 | Every mic failure maps to sentence + next action + demoted footnote; lifecycle losses same vocabulary. |
| 10 | Help and Documentation | 3 | 14 param help lines real; no glossary/README link; hover-only discovery. |
| **Total** | | **33/40** | **Good** |

## Refinement Verification

1. Occlusion (P1) — RESOLVED as paint defect, PARTIALLY RESOLVED operator-facing: 0% overlap verified live (was 50%/47%); but OUT bottom edge y≈1308 in a 726px-tall visible canvas with default preset expanded — OUT meter/safe-output note below the fold until scrolled/collapsed.
2. Plain-language params (P1) — RESOLVED: 14/14 titles, sr-only spans, aria-describedby verified live; high copy quality; hover/title-only discovery.
3. Operator-voice error recovery (P1) — RESOLVED: DOMException map + secure-context case + generic fallback, start/switch variants, footnote demoted, aria-live strip, lifecycle losses reuse vocabulary.
4. 11px floor (P2) — RESOLVED: 0 undersized findings (was 5); live sweep found no leaf text below 11px.
5. Pre-Start honesty (P2) — RESOLVED: state-aware hints ("Press Start to power on"), not-allowed cursor, dashed affordance retired while gated, drag copy flips at Start success.

Hardening wave: bypassed-canvas indication verified live (chain 0.55 + BYPASSED ::after, anchors opacity 1, cleared on session loss, single-source read); hidden-tab fallback warning in calm amber/status register; 10-tool surface produced no new UI strain (?dev-gated).

## Design Specificity Verdict

**LLM assessment: genuinely authored, strengthened by the wave.** Error register, recovery-hint mechanics (CSS keyed to Start's disabled state), and the mono BYPASSED line are native to the Touring Rack vocabulary; not category-interchangeable.

**Deterministic scan:** CLI full-parser (temp copy, parser deps outside repo): 4 findings — overused-font (roboto), flat-type-hierarchy (1.5:1), side-tab (sortable-chosen inset stripe), repeating-stripes. Browser overlay (fresh headless tab, injection succeeded, console "[impeccable] 9 anti-patterns found", 17 overlay elements): wide letter-spacing ×2 (sr-only spans), positioned-child-clipped ×2, border-accent ×6 (cards), overused-font shares, flat hierarchy. Baseline delta: undersized text 5→0, text-occlusion 2→0.

**Adjudicated keeps (re-flagged, upheld):** family-edge borders, detent-tick gradient, roboto in system stack, sr-only wide tracking, register-carried hierarchy. **New-rule adjudication:** positioned-child-clipped on #chain-layout — false positive; the clipping is the VIS-7 bounded scroll canvas by design (the flagged positioned children are the z-guarded anchors the occlusion fix added).

**Visual overlays:** injection succeeded in a fresh automated tab; no user-visible [Human] tab (no native browser-presentation tool exposed; overlay verified via screenshot at /tmp/impeccable-critique-b/overlay-live.png).

## Overall Impression

The five refinements landed in-vocabulary; the console now reads as a finished touring instrument whose failure states teach recovery. Score moved Acceptable→Good. Remaining: OUT's scroll-position dependence, pointer-only node addition, native-dialog nits.

## What's Working

1. The recovery-hint mechanism (visibility keyed to Start's disabled state — zero drift possible).
2. The bypassed-canvas indication (single-source read, session-loss clearing, meters stay full-strength).
3. The error-copy map (six DOMException names + secure-context + honest generic, start/switch variants).

## Priority Issues

1. **[P2] OUT meter below the fold in default expanded live state** — occlusion fixed, but output health depends on scroll position. Fix: persistent OUT presence in the always-visible region (pinned compact readout or auto-collapse default preset on Start). Suggested command: $impeccable layout
2. **[P2] Node addition unreachable by keyboard/screen reader** — palette chips non-focusable divs. Fix: button semantics + add-to-chain activation. Suggested command: $impeccable adapt
3. **[P2] Native prompt/confirm break the console vocabulary mid-show** — Fix: in-panel inline naming + two-step Delete. Suggested command: $impeccable clarify

## Persona Red Flags

**Marco (live operator)**: param help hover-only (no discovery affordance); OUT below fold in default state; "AGENT —" chip still reads broken when WebMCP absent; "Classic Karaoke" twice in dropdown.
**Sam (a11y)**: aria-live status + announced param help are real gains; node addition still pointer-only; grip affordance hover-title-only.
**Alex (power user)**: no keyboard add/reorder, no numeric fader entry, collapse resets on loads (accepted v1).

## Minor Observations

- No visible affordance for the param help layer (a "?" seam or first-run tip would raise discovery).
- Switch-error copy can ellipsize its next-action clause under the 20rem status cap on long device labels.
- Detent ticks faintly noisy (adjudicated keep).
- Two favicon-class 404s in console on fresh profile.
- Suspended-context auto-recovery appears soundly guarded (stale-completion checks).

## Questions to Consider

- If OUT is the one thing the operator must always see, should it live inside the scroll at all?
- Is the next teaching surface a first-run tip ("hover any knob") rather than a glossary?
- One more refinement pass for the P2 scatter, or close the phase?
