---
target: index.html
total_score: 35
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 0
p2_count: 1
timestamp: 2026-08-29T17-20-19Z
slug: index-html
---
⚠️ DEGRADED: single-context (no sub-agent/Task tool exposed in this session). Assessment A completed and recorded in full before detector output entered context; Assessment B ran second. Round-2 closing run; baselines 27/40 (2026-08-29T05-41-33Z) and 33/40 (2026-08-29T16-31-45Z).

# Design Critique — index.html (Karaoke Chain Builder) — Round-2 Closing Run

Mode: Operate. Target: index.html + styles/main.css, src/canvas.js, src/param-controls.js, src/presets-ui.js, src/main.js, src/agent-ui.js, src/meters.js, src/meter-taps.js. Intent sources: PRODUCT.md, DESIGN.md, .impeccable/surfaces/index-html.md, .impeccable/design.json.

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | Pinned OUT footer keeps output health on screen in the default expanded live state, undimmed while bypassed. |
| 2 | Match System / Real World | 3 | Help discovery still hover-only (the user-selected "?" seam affordance remains unfunded). |
| 3 | User Control and Freedom | 3 | Inline naming + two-step Delete; manual slider edits still lack undo. |
| 4 | Consistency and Standards | 4 | Native dialogs gone entirely (zero dialogs fired across all flows). |
| 5 | Error Prevention | 3 | Armed Delete, honest chip gating, terminal-limiter invariant; +24 dB gain and limiter removal mid-show unguarded. |
| 6 | Recognition Rather Than Recall | 4 | Node addition is a real button with action-phrase name, tab-reachable, keyboard-activatable (verified live). |
| 7 | Flexibility and Efficiency | 3 | Keyboard add is an accelerator; no numeric fader entry, no keyboard reorder. |
| 8 | Aesthetic and Minimalist Design | 4 | Footer earns its row; zero-overlap invariant untouched; detent ticks the only faint noise. |
| 9 | Error Recovery | 4 | Unchanged resolved state; the surface's strongest register. |
| 10 | Help and Documentation | 3 | 14 help lines announced via aria-describedby; no visible affordance. |
| **Total** | | **35/40** | **Good — one point shy of Excellent** |

## Refinement Verification

1. R2-1 Pinned OUT footer — RESOLVED. Verified live (default expanded, 1440x900): footer at y763–816, in-viewport without scrolling (OUT anchor itself still y≈1308 in the scroll region); live value; opacity 1 under Bypass while chain dims to 0.55; outside .canvas so occlusion guard invariant untouched. Horizontal-mode scroll-away documented as the vertical default's guarantee.
2. R2-2 Keyboard/SR node addition — RESOLVED. All six chips real <button aria-label="Add X to chain">, disabled pre-Start, enabled at Start; Enter-add verified live through the same commit chokepoint as drag (autosave/unsaved-dot/revision bump shared); spacebar guard verified (Space on focused chip does not toggle Bypass); append-before-terminal-limiter preserves the safe-output invariant.
3. R2-3 Inline Save-As + two-step Delete — RESOLVED. window.prompt/confirm absent from source (grep) and zero native dialogs fired (playwright hook). Naming row: focused+selected on open, Enter commits, Escape cancels with focus returned, 1–40-char bound, end-to-end save verified. Delete: arms to "DELETE?" (verified), 5s expiry disarms (verified), Escape disarms, factory presets get the quiet refusal note.

No regressions: pre-Start honesty, error copy, bypass indication, 11px floor, plain-language help re-verified in passing; occlusion geometry clean.

## Design Specificity Verdict

**LLM: authored and converging.** The footer is a second VIEW of one metering scene (one feed, one ballistics) in the silkscreen/mono registers; armed "DELETE?" uses the reserved safety-edge vocabulary. Not category-interchangeable.

**Deterministic scan:** CLI full-parser: 4 findings (overused-font roboto, flat-type-hierarchy 1.5:1, side-tab stripe, repeating-stripes) — identical to round-1 close, all adjudicated keeps. Browser overlay (fresh headless tab, injection succeeded, console "[impeccable] 9 anti-patterns found"): wide-tracking (agent chip sr-only), clipped-overflow (#chain-layout), border-accent x6 (cards), body overused-font/flat-hierarchy/gradient-text/monotonous-spacing, text-occlusion x2 (param-help sr-only spans).

**Adjudicated keeps upheld:** family edges, detent ticks, roboto-in-stack, sr-only tracking, register-carried hierarchy, bounded-scroll-canvas clip. **New adjudications:** text-occlusion x2 on sr-only spans — false positive (sr-only clip pattern); gradient-text — false positive (zero background-clip:text in stylesheet, grep-verified); monotonous-spacing — advisory, subsumed by flat-hierarchy adjudication.

**Visual overlays:** injection succeeded in a fresh automated tab; no user-visible [Human] tab (no native presentation tool exposed; screenshot /tmp/impeccable-critique2-b/overlay-live.png).

## Overall Impression

All three round-2 refinements landed cleanly, in-vocabulary, without regressions; no known P1/P2 layout/a11y/dialog defects on primary flows remain. What is left is a short, already-scoped polish list.

## What's Working

1. The pinned OUT footer's one-scene-two-views design (two readouts can never disagree).
2. The keyboard-add path's chokepoint discipline (input modalities cannot diverge in state side effects).
3. The armed Delete (5s expiry + Escape + factory refusal — right destruction choreography under time pressure).

## Priority Issues

1. **[P2] Unguarded loud-path actions: +24 dB gain and limiter removal mid-show** (standing from run 1). Suggested command: $impeccable harden
2. **[P3] Param-help discovery affordance absent** (user-directed "?" seam, unfunded). Suggested command: $impeccable clarify
3. **[P3] Cosmetic residue: "AGENT —" placeholder read; duplicate "Classic Karaoke" preset labels.** Suggested command: $impeccable polish

## Persona Red Flags

**Marco**: OUT health always visible (the round's biggest mid-show win); hands stay on the panel for naming; residual: help discovery cue, duplicate preset labels.
**Sam**: build flow fully operable (chip buttons + Enter add; labeled, focus-managed naming row); residual: keyboard reorder, grip hover-title-only.
**Alex**: Enter-add is a real accelerator; no numeric fader entry or duplicate-node action.

## Minor Observations

- Global spacebar handler now button-aware; verified with the chip case.
- Footer's fixed footprint: no layout pumping observed.
- Detent-tick faintness and two favicon-class 404s remain.
- Naming row pre-fill matches the old prompt's suggestion semantics.

## Questions to Consider

- Is another UI pass worth more than closing the phase and refreshing README/DESIGN to describe what now exists?
- If one more pass: ?-seam + AGENT-chip pair, or the loud-path guard?
